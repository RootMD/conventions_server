{% extends 'base.html.twig' %}

{% block title %}Gra{% endblock %}

{% block body %}
    <h1>Gra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gra.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ gra.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gra_index') }}">back to list</a>

    <a href="{{ path('gra_edit', {'id': gra.id}) }}">edit</a>

    {{ include('gra/_delete_form.html.twig') }}
{% endblock %}
