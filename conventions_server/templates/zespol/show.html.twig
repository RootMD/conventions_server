{% extends 'base.html.twig' %}

{% block title %}Zespol{% endblock %}

{% block body %}
    <h1>Zespol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zespol.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ zespol.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zespol_index') }}">back to list</a>

    <a href="{{ path('zespol_edit', {'id': zespol.id}) }}">edit</a>

    {{ include('zespol/_delete_form.html.twig') }}
{% endblock %}
