{% extends 'base.html.twig' %}

{% block title %}Zespol index{% endblock %}

{% block body %}
    <h1>Zespol index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nazwa</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for zespol in zespols %}
            <tr>
                <td>{{ zespol.id }}</td>
                <td>{{ zespol.nazwa }}</td>
                <td>
                    <a href="{{ path('zespol_show', {'id': zespol.id}) }}">show</a>
                    <a href="{{ path('zespol_edit', {'id': zespol.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
