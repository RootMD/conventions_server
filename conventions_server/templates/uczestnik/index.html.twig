{% extends 'base.html.twig' %}

{% block title %}Uczestnik index{% endblock %}

{% block body %}
    <h1>Uczestnik index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imie</th>
                <th>Nazwisko</th>
                <th>Nick</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for uczestnik in uczestniks %}
            <tr>
                <td>{{ uczestnik.id }}</td>
                <td>{{ uczestnik.imie }}</td>
                <td>{{ uczestnik.nazwisko }}</td>
                <td>{{ uczestnik.nick }}</td>
                <td>
                    <a href="{{ path('uczestnik_show', {'id': uczestnik.id}) }}">show</a>
                    <a href="{{ path('uczestnik_edit', {'id': uczestnik.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('uczestnik_new') }}">Create new</a>
{% endblock %}
