{% extends 'base.html.twig' %}

{% block title %}Konkurs{% endblock %}

{% block body %}
    <h1>Konkurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ konkur.id }}</td>
            </tr>
            <tr>
                <th>Nagroda</th>
                <td>{{ konkur.nagroda }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ konkur.data ? konkur.data|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ konkur.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('konkurs_index') }}">back to list</a>

    <a href="{{ path('konkurs_edit', {'id': konkur.id}) }}">edit</a>

    {{ include('konkurs/_delete_form.html.twig') }}
{% endblock %}
