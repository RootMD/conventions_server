{% extends 'base.html.twig' %}

{% block title %}Konwent{% endblock %}

{% block body %}
    <h1>Konwent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ konwent.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ konwent.nazwa }}</td>
            </tr>
            <tr>
                <th>Miasto</th>
                <td>{{ konwent.miasto }}</td>
            </tr>
            <tr>
                <th>Lokalizacja</th>
                <td>{{ konwent.lokalizacja }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ konwent.data ? konwent.data|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('konwent_index') }}">back to list</a>

    <a href="{{ path('konwent_edit', {'id': konwent.id}) }}">edit</a>

    {{ include('konwent/_delete_form.html.twig') }}
{% endblock %}
